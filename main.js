function letCarsin(){

let carCounter = 0;
const maxCars = 1000000;

while (carCounter < maxCars) { window.alert("Car number " + carCounter + " is entering");
    carCounter +=100000;
    }
window.alert("Full")
}